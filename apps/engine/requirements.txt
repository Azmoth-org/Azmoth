# Pinned. These are the versions the engine is developed, tested and shipped against; the receipt
# hash and the audit trail both record the solver versions, so an unpinned bump would silently
# change what a receipt means.
fastapi==0.140.0
uvicorn[standard]==0.51.0
pydantic==2.13.4
pydantic-settings==2.14.2

# The optimiser. Same major/minor as the POC: a different Clingo can legitimately pick a different
# model among equally optimal ones, which would move a golden snapshot.
clingo==5.8.0

# The Datalog engine is the `souffle` BINARY, not a Python package — see the Dockerfile and README.

httpx==0.28.1

# Multipart form parsing, required by Starlette to decode `UploadFile`. Added for
# `POST /api/v1/padnext/batch`, which takes many files in one request — the single-file
# `/padnext/audit` endpoint takes raw bytes and deliberately still needs none of this.
python-multipart==0.0.20

# -- persistence -------------------------------------------------------------------------------
# Proposals and the audit log live in Postgres in production. Pinned for the same reason as the
# solvers: a schema this service migrates must not move under it between environments.
#
# `sqlalchemy[asyncio]` pulls in greenlet, which is what lets the sync ORM internals run under an
# async driver. asyncpg is the production driver; aiosqlite is the local/test one and is NOT
# optional — DATABASE_URL defaults to sqlite+aiosqlite, so the suite needs it.
sqlalchemy[asyncio]==2.0.52
asyncpg==0.31.0
aiosqlite==0.22.1

# Schema migrations. The container runs `alembic upgrade head` before uvicorn, so alembic is a
# runtime dependency of the image, not a dev tool.
alembic==1.19.1

# -- development / test ------------------------------------------------------------------------
pytest==9.1.1
pytest-asyncio==1.4.0
