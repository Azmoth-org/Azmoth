# Where the GOÄ engine (apps/engine) is reachable. Server-side ONLY — deliberately not
# NEXT_PUBLIC_, so the browser never learns the engine's address and cannot call it directly.
# Every request goes through the /api/engine/* route handlers.
ENGINE_BASE_URL=http://localhost:8000
